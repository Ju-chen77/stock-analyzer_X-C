# CHANGELOG

所有版本的变更记录，按时间倒序排列。

---

## [app_V5.9] — 2026-06-26

### 新增

- **新三板主营构成（业绩检验 Part D）**：此前新三板 `segment` 置空 → Part D 空白。新增 `neeq_data.parse_segment()`，从年报「按产品分类分析 / 按行业分类分析」表抽取各产品的营收 / 营业成本 / 毛利率（自动合并折行的产品名，如「移动空」+「调」→ 移动空调），逐份年报解析拼出多年；`app_V5.py` 的 NEEQ 分支用解析出的 segment 填充 Part D
  - 实测 874628 欧伦电气：Part D 显示 除湿机 / 移动空调 / 车载冰箱 / 热泵系列 / 配件及其他 / 其他业务收入（2025 + 2024），收入 / 毛利率 / 成本三图正常
  - 依赖年报含「按产品分类分析（√适用）」表；公司标「□不适用」则仍空白（数据确无，与沪深京缺主营构成同理）

---

## [app_V5.8] — 2026-06-26

### 修复

- **图表横轴去掉无意义的小数年份刻度**：年份型 x 轴（柱/线图）此前被当作连续数值轴，自动插入 `2022.5 / 2023.5` 等没有数据的刻度。给默认 `LAYOUT_BASE.xaxis` 加 `dtick:1`（每 1 年一刻度）+ `tickformat:'d'`（整数、去千分逗号）→ 只显示实际数据年份 `2023 / 2024 / 2025`，并保留线性轴使年份按旧→新升序排列；数值类图（单变量冲击 / 分布直方图）与 K线 / 热力图各自有独立 x 轴设置，不受影响

---

## [app_V5.7] — 2026-06-26

### 新增 · 新三板（NEEQ）支持（年报解析基本面 + 跨分类行业对比）

新三板无结构化财务接口、push2 行情被代理墙、akshare 本版无新三板函数、年报 PDF 在 CDN 有 JS 反爬 →
改用东财**公告内容 API 的服务端抽取文本**通路落地（阶段六「年报解析」的文本版）：

- **`neeq_data.py`（新增）**：
  - 发现：东财新三板公告列表 `/api/gg/list`（`securitycodes` 翻页）筛「年度报告」→ 年份 + `art_code`
  - 取文本：内容 API `np-cnotice-stock` 分页取年报全文（绕开 PDF 反爬）
  - 解析：定位合并资产负债表 / 利润表 / 现金流量表，逐行抽取标准报表项（剔除母公司表、其中：等前缀）
  - 组装：多份年报按「本期优先」合并 → 拼出多年；输出为应用现有 `raw`（新浪报表口径 DataFrame，`_date=YYYY1231`），**复用全部 `compute_*`**；30 天缓存
- **`app_V5.py`**：`api_analyze` 加 NEEQ 兜底——北证段代码且沪深京无数据时走 neeq 重算；`info.market=NEEQ`、`annual_only=True`，现价/估值/量价置空
- **行业对比纳入新三板**：`neeq_data.parse_industry` 从年报「行业分类」段抽所属行业（CSRC 口径）→ 经 `EM_TO_SW` 关键词表映射到申万二级（复用北交所跨分类）→ 借该申万行业沪深同行对比；目标股财务用年报解析值注入（`neeq_metrics`，因 `financial_analysis_indicator` 对新三板返回空）
- **实测 874628 欧伦电气**：3 年（2023–2025）业绩检验 / 归因 / 排雷 / 盈利预测正常；行业对比「家用空气调节器制造」→ 申万「白色家电」，跨分类对比正常
- **口径声明（写进结果）**：仅年报无季度；无现价 / PE-PB / 量价；行业对比为跨分类，且新三板年度数与沪深同行最新一期（可能季度）口径不一致 → 比率分位系统性偏高，看相对方向；解析依赖年报版式，较脆弱，缺失项留空不臆造

### 调整

- **期间标签改为前缀式**：半年报 `2023H1` → **`1H2023`**，季报 → `1Q2023` / `3Q2023`，年报仍显示年份；`fmtPeriod` 一处改、全图表 x 轴与时期条生效

---

## [app_V5.6] — 2026-06-22

### 新增 · 行业对比纳入北交所

- 申万 legulegu 成分**不含北交所**（实测 5198 只全为沪深）→ BSE 股原本一律「不可用」。新增东财 → 申万兜底：
  - BSE / 申万未覆盖股取**东财行业**（emweb F10）→ 经 `EM_TO_SW` 关键词表映射到**申万二级** → 借该行业（沪深为主）同行池做**跨分类对比**，目标股注入池中（权重置空，市值维度不参与）
  - 命中关键词越具体越优先；无法匹配则诚实声明不可用，不乱归类（新增 `_match_sw_by_em()`）
  - 前端加**跨分类提示横幅** + 注释：北交所规模通常偏小 → 市值 / 规模维度不可比、行业分位系统性偏低，盈利 / 毛利 / 周转等比率维度更可参考
  - `industry_comparison` 仅缓存 `available=True` 结果，避免历史「不可用」缓存遮盖；`/api/industry` 透传东财行业
  - 验证 920222 益坤电气：东财「电气设备-输变电设备」→ 申万「电网设备」，41 只沪深同行，跨分类对比完整渲染

### 北交所支持审计

- **代码层面已全面 BSE-aware**：交易所路由（统一 `_exchange`）/ 三大报表 / 财务指标 / 名称 / 行业 / 行业对比 / 搜索建议均支持
- 余下限制为**数据源对新上市 BSE 股的覆盖**：现价 / PE-PB 估值 / K线量价依赖新浪日线，新股暂无日线 → 优雅降级（显示「—」/「暂无数据」，不报错）；老 BSE 股（83/87/88 段）有日线则可正常使用

---

## [app_V5.5] — 2026-06-22

### 修复

- **北交所代码被误判为上证**：`920` 段（如益坤电气 `920222`）以 `9` 开头，原「`6/9` → 上证」路由把它发到 `sh` / `SH`，新浪行情与东财 F10 都查错交易所 → 名称 / 行业全部取不到。新增 `_exchange()` 统一判定交易所（`4/8/920 → BJ`，先于 `6/9 → SH`），收敛 `to_sina_code` 与两处 emweb 前缀；搜索建议放行 `bj`。修复后 `920222` 正常显示「益坤电气 / 电气设备-输变电设备」并可完整分析（新股暂无日线 → 现价显示「—」）
- **业绩检验单图溢出卡片**：`营业收入 & 归母净利润` 等独占整行的图，首次在容器宽度未就绪时渲染，Plotly 回退到 700px 默认宽 → 横向溢出半宽卡片。`renderTab` 末尾加双 `requestAnimationFrame` → `Plotly.Plots.resize` 兜底，布局稳定后按卡片实际宽度重算（实测由 700px 收敛到 517.5px，恰好贴合 558px 卡片）

---

## [docs] — 2026-06-22

### 文档

- **重写 CLAUDE.md 项目框架**，使其与实际实现对齐并扩展为完整体系：
  - 数据源由 **Choice 终端（EMQuantAPI）改为 akshare**（与现有代码一致）
  - 明确完整分析链路：业绩检验 → 业绩归因 → 验证排雷 →（行业对比横向支撑）→ 盈利预测 → 估值 ↕ 量价印证
  - 新增文档体系树（9 份框架文档）、分六阶段开发计划、`settings.py` 配置约定、代码 ↔ 文档双向绑定原则
  - 补充设计原则（缓存优先 / 红灯一票否决 / 趋势>绝对值 / 行业差异化 / CAS 口径）与 akshare 已知短板应对表

---

## [app_V5.4] — 2026-06-22

### 新增 · 行业对比层（Tab ⑦ 行业对比）

- 按《行业对比框架》新增 `industry_compare.py` + 懒加载 `/api/industry` 路由 + 前端 Tab：
  - **A 精选同行对比**：8 维相似度筛 Top 15（「x/8 项相似」+ 相似维度徽章）+ 行业龙头锚定（🔒）+ 精选同行 / 全行业双基准中位数；相似度 <3 时降级提示
  - **B 行业相对位置**：12 项行业分位条（马卡龙配色，反向指标已反转）+ 6 轴雷达（本公司 vs 行业中位）+ ROE / 净利率行业分布直方图（标注本公司）+ 四支柱综合定位标签（绩优成长 / 稳健盈利 / 激进成长 / 行业落后 / 均衡中游）

### 数据架构（代理环境实测后的可行性调整）

- **东财 push2 接口全部被代理阻断**（`stock_individual_info_em` / `stock_board_industry_*` / `stock_zh_a_spot_em`）→ 全面改走**申万二级**路径（legulegu 源，可用）
  - 遍历 131 个二级行业构建「证券代码 → 申万行业」反查图（实测 5198 只 / 0 失败 / ~29s），落盘缓存 90 天（L1）
  - `index_component_sw` 的「最新权重」作**市值 / 龙头代理**（规避被阻断的行情源），**总资产**作规模代理，**近 4 期营收同比**近似成长性；毛利率缺失退主营业务利润率
  - 单行业按权重取前 40 只分析（控耗时）；个股指标 / 行业池 / 最终结果三级缓存（7 天）；剔除 ST/*ST 与 ROE 极端值，用中位数
- **暂缓**：5 年行业分位趋势（需逐年重抓全行业历史，开销过大）

### 工程

- `.gitignore` 补充 `data/`、`output/`（CLAUDE.md 要求但原先未忽略）及本地临时调试脚本（`_debug*.py` / `_test_*.py` / `_probe*.json`）

---

## [app_V5.3] — 2026-06-18

### 修复

- **图表横向滚动条**：业绩检验（Tab①）资产/负债结构两列图表会把页面撑出横向滚动条。
  根因——`render()` 中 `renderTab('t1')` 在 `outEl` 仍为 `display:none` 时执行，Plotly 读到容器宽度 0、回退到默认 ~700px；叠加 CSS Grid 的 `1fr`（即 `minmax(auto,1fr)`）以内容固有宽度作为列下限，两列被撑出视口。
  修复：① `render()` 先显示 `outEl` 再渲染 Tab①；② `.charts-2col` / `.charts-3col` 改用 `minmax(0,1fr)`、`.chart-wrap` 加 `min-width:0` 作为兜底——任何标签页都不会再撑出横向滚动条

### 调整（配色 · 马卡龙色系）

- **堆积面积图（资产 / 负债结构）**：高饱和色板改为低饱和**马卡龙色系**（玫瑰粉 `#F2A6B3` / 薄荷绿 `#A8DEC0` / 天空蓝 `#A9C7EC` / 薰衣草紫 `#C9B6E4` / 蜜桃橙 `#F7C6A3` / 水鸭青 `#9BD6D2` / 奶油黄 `#F5E1A4`）；去掉色块间分界线（`line.width=0`），色块无缝相接
- **热力图**：敏感性热力图（Tab⑥）与红黄绿状态图（Tab③）统一为马卡龙 红 `#F49096` → 黄 `#F8D777` → 绿 `#8FD3A6`（较堆积图略提饱和）；状态图保留灰 `#e2e8f0` 表示无数据

---

## [app_V5.2] — 2026-06-18

### 界面改版 · 浅色数据风（dark → light）

- **整体配色由深色未来风改为浅米色数据风**，聚焦数据与图表可读性
  - 页面底色 `#faf8f5`；侧栏 / 顶栏 / 股票栏 / 页脚统一 `#f5f2ed` 米色系（避免白色组件与米色内容割裂）；卡片 `#fff`
  - 字体全局改为等宽 `IBM Plex Mono`
  - 图表改用高饱和色板：青 `#0891b2` / 琥珀 `#d97706` / 蓝 `#2563eb` / 紫 `#7c3aed` / 红 `#dc2626`
  - 卡片去边框，改用浅投影 `box-shadow:0 1px 3px rgba(0,0,0,.06)`；状态卡保留左侧 3px 色条（绿 / 橙 / 红）
  - Plotly `LAYOUT_BASE` 适配浅底：文字 `#475569`、网格 `#e2e8f0`、背景透明
  - K 线沿用 A 股习惯：涨红 `#dc2626` / 跌绿 `#16a34a`
- **图表两列布局**：新增 `.charts-2col` 栅格，相关图表两两并排；单张图表也约束为半宽卡片，消除右侧冗余空白（内容区维持原宽度）；≤720px 自动回落单列
- **现金流组合图横轴对齐**：`.cfpattern-strip` 左边距对齐 Plotly 左 margin（52px），使下方时期标签与图表横轴对齐

### 说明

- 新增 `app_V5.py` + `templates/index_v5.html`（浅色版工作文件）；旧版 `app_V4.py` / `templates/index.html` 保持不动，便于回退对照

---

## [app_V5.1] — 2026-06-18

### 新增

- **盈利预测层（Tab ⑤ 盈利预测）**：按「盈利预测框架」新增预测模块，位于估值之前的前置环节，永远输出区间与情景、假设全部显式化。新增 `compute_forecast()`，复用主分析已抓取的历史利润表，无额外网络请求
  - **关键假设面板**：营收增速（3/5年CAGR加权）、毛利率、期间费用率（含规模效应摊薄）、有效税率、归母净利率锚，全部显式列出便于复盘
  - **三档情景**（悲观/基准/乐观）：营收→毛利→费用→税→归母净利润→EPS，并衔接历史中位 PE 给出对应目标价
  - **基准 3 年轨迹**：复利外推 T+1/T+2/T+3 营收与归母净利润
  - **敏感性矩阵**：营收增速 × 毛利率 5×5 热力图 → 归母净利润
  - **单变量冲击**：营收增速/毛利率/费用率/有效税率单独变动对归母净利润的影响（直观印证"毛利率假设错 2pct 比营收错 10% 影响更大"）
- 框架文档入库：`盈利预测框架.md`（含落地说明，记录可行性调整）

### 可行性调整

- **方法 C（量价模型）已从框架删除**：分产品销量/ASP/行业规模/市占率三种数据源（新浪/东财 emweb/akshare 其余接口）均无法提供，无法落地，已从 `盈利预测框架.md` 移除；收入预测以方法 A + 方法 B 为准
- **方法 B（一致预期）/ 预期差量化暂未实现**：`stock_profit_forecast_em` 等一致预期接口在本地代理环境不可用。本模块以方法 A（历史 CAGR）+ 历史假设落地，方法 B 待可直连环境补充
- **净利润推导改用「归母净利率锚」**：以当前实际归母净利率为基准，按毛利率/费用率相对基准的偏离（税后）调整，而非纯自下而上 营收×毛利率−费用−税——后者对投资收益/减值/少数股东损益占比大的控股型公司（如 TCL，归母 45 亿但经营口径仅 ~16 亿）会系统性失真

### 调整

- Tab 顺序：盈利预测置于 ④估值区间 之后、量价印证之前，显示为 ⑤；量价印证顺延为 ⑥（内部 id 保持 t5/t6 不变）

---

## [docs] — 2026-06-14

### 文档

- **`量价数据框架.md` 同步实际落地情况**：在「五、所需数据汇总」补充数据端口实际应用说明——区分实际采用端口（新浪 `stock_zh_a_daily` qfq / `stock_zh_index_daily`）与被代理阻断的东财 push2 接口（主力/北向/龙虎榜/涨停池/融资/大宗/东财行业指数）；「七、开发优先级」补充各模块落地状态（✅已实现 / ⚠️部分 / ❌受阻），与 Tab ⑤ 量价印证实现保持一致

---

## [app_V5.0] — 2026-06-14

### 新增

- **量价印证层（Tab ⑤ 量价印证）**：按「量价数据框架」新增市场印证层，横切基本面，不预测股价只做共振/背离判定。新增 `compute_market()` + `/api/market` 懒加载接口（切到 Tab ⑤ 才请求，不拖慢主分析）
  - **基础行情**：日 K（前复权）蜡烛图 + 成交量 + 均线 MA5/20/60（A 股红涨绿跌），数据源新浪日线（含换手率）
  - **场景 C 估值修复确认**：估值低位（PE/PB ≤30 分位）× 量能放大（近5日均量较前期 +30%）× 趋势确认（站上 60 日线 + 多头排列）三重共振 → 估值修复启动 / 价值陷阱 / 信号不足
  - **场景 D 相对强度**：个股 vs 上证综指近 60 交易日涨幅差（跑赢/跑输大盘），数据源新浪指数
  - **基本面 × 量价 四象限**：以排雷一票否决结论为基本面轴 × 量价方向 → 共振做多 / 共振警示 / 价值陷阱 / 静默回避
- 框架文档入库：`量价数据框架.md`

### 说明

- **资金流向（北向持股 / 主力净流入 / 龙虎榜 / 融资融券 / 大宗交易）暂未接入**：经实测，这些数据的 akshare 接口（`stock_individual_fund_flow`、`stock_hsgt_hold_stock_em` 等）依赖东财 push2，被本地代理阻断（与历史 PE 改用新浪源同因）。本轮量价层全部基于代理可达的新浪日线/指数数据构建，契合框架"保持简单、只用最基础量价指标"原则；资金流向待可直连环境补充

---

## [app_V4.9] — 2026-06-14

### 修复

- **行业名称仍不显示（V4.8 修复未生效）**：上一版改用的 `push2.eastmoney.com` 行情接口在本地代理环境被阻断（ProxyError，与早前 `push2his` 同因），导致行业仍为空。改用 `emweb.securities.eastmoney.com` F10 接口（与「主营构成」同源、代理可达）的 `CompanySurvey` 取行业：优先东财 EM2016 三级分类（去重连级，如「电子设备-电子元件」「食品饮料-饮料-白酒」），退 CSRC 大类；名称取 `SECURITY_NAME_ABBR`

---

## [docs] — 2026-06-14

### 新增

- **入库两份框架文档**：`业绩检验框架（补充）.md`、`验证排雷框架（akshare）.md`，对应近期 Tab ① 业绩检验三层重组与 Tab ③ 验证排雷重组的方法论依据；更新文末「框架文档」索引

---

## [app_V4.8] — 2026-06-14

### 修复

- **股票所属行业不显示**：`ak.stock_individual_info_em` 存在 pandas bug（Length mismatch: Expected 3, got 2），导致行业一直为空显示「—」。新增 `_get_em_info()` 改用东财 push2 行情接口（f127 行业 / f58 名称）获取，行业正常显示（如 TCL科技→光学光电子、贵州茅台→白酒）

### 变更

- **标题改为「财报透析」**：原「三段式财报透视」，同步更新页面 `<title>`、应用主标题与 GitHub Pages 落地页
- **删除主标题下方副标题**：移除「业绩检验 · 业绩归因 · 验证排雷」小字

---

## [app_V4.7] — 2026-06-14

### 重构

- **Tab ① 业绩检验按「业绩检验框架（补充）」三层重组**：Part A 盈利能力 / Part B 业绩自洽性 / Part C 现金流验证 / Part D 主营构成 / Part E·F 资产负债堆积，在原有版本基础上增补内容

### 新增

- **A 层盈利能力增补**：
  - KPI 卡片增加「扣非净利率」「ROIC」（共 7 项）
  - 利润率趋势图增补「营业利润率」「扣非净利率」（毛利率/营业利润率/净利率/扣非净利率四线）
  - 新增「回报率趋势」图：ROE / ROA / ROIC（排除杠杆看真实回报）
  - 新增「增长质量判读」：营收增速 vs 扣非净利润增速，识别低基数效应与非经常损益拉动
- **B 层业绩自洽性**（新增章节）：营业收入 vs 合同负债双柱+YoY 图（未来订单领先指标）；应收/存货自洽性仍见 Tab ③
- **C 层现金流验证增补**：
  - 新增「自由现金流 FCF」图（经营现金流 − 资本开支），红绿区分正负
  - 新增「三类现金流组合」图（经营/投资/筹资）+ 逐年 +/− 组合标签条，映射 8 种公司阶段（健康成熟/成长扩张/靠融资生存/变卖资产等）
- **扣非净利润数据源**：新增 `_fetch_deduct_indicator()`，额外调用 `stock_financial_analysis_indicator`（新浪源）补齐扣非净利润，失败容错不阻塞主流程

### 说明

- 行业对比（毛利率 vs 同行中位数、行业分位雷达图）需循环抓取同行数据，有阻断风险，留待后续单独接入

---

## [app_V4.6] — 2026-06-13

### 移除

- **验证排雷删去两项指标**：移除「商誉占比」（资产端）与「有息负债/货币资金」（偿债杠杆），排雷指标从 10 项精简为 8 项（资产端 4 + 偿债杠杆 4），热力图同步减为 8 行

---

## [app_V4.5] — 2026-06-13

### 重构

- **Tab ③ 验证排雷按「验证排雷框架」重组**：从 5 项扁平指标升级为 A 资产端 / B 偿债杠杆 两大分组维度（共 10 项），全部基于已抓取的三大报表结构化数据

### 新增

- **一票否决总判定卡片**（D-2）：任一红灯 → 不通过（估值作废）；≥3 黄灯 → 警示（打折观察）；无红灯且黄灯≤2 → 通过；全绿 → 优质。顶部醒目展示 🔴🟡🟢 计数
- **红黄绿历史热力图**（D-3）：10 项指标 × 近 6 年的状态矩阵，颜色随年份漂移直观呈现风险演变趋势（"看演变，不看快照"）
- **资产端 5 项**：商誉占比、收入质量（应收−营收增速）、库存压力（存货−营收增速）、其他应收占比、研发资本化（Δ开发支出口径）
- **偿债杠杆 5 项**：资产负债率、**有息负债/货币资金（康美/康得新造假红线）**、速动比率、利息保障倍数、现金流偿债覆盖
- **行业差异化阈值提示**（E）：说明银行/地产/证券/周期股需专门规则，治理维度（质押/审计/监管）需额外数据源待接入

### 修复

- 研发资本化比例口径修正：从「累计开发支出余额 / 研发费用」（存量÷流量，导致低研发企业如茅台误报红灯）改为「Δ开发支出 /（研发费用+Δ开发支出）」（流量÷流量），茅台从误报"不通过"修正为"通过"

---

## [app_V4.4] — 2026-06-13

### 重构

- **Tab ② 业绩归因四层递进重组**：按「业绩归因框架」重新组织为第一层 ROE 杜邦分解、第三层利润率拆解、第四层现金流验证三大模块

### 新增

- **杜邦贡献度量化卡片**（第一层）：计算 ROE 同比变化中净利率、周转率、杠杆各贡献多少 pct，标注主导因子
- **利润率瀑布图**（第三层）：从毛利率出发，逐项展示销售/管理/研发/财务费用的拖累，最终到净利率，Plotly waterfall 可视化
- **利润率趋势折线**（第三层）：毛利率 / 净利率 / 营业利润率 / 扣非净利率 四线对比
- **现金流验证总结**（第四层）：三组验证卡片（收入真实性、利润真实性、FCF）+ 通过/关注/不通过 判定，归因结论必须通过验证才能采信

### 改进

- `compute_attribution()` 扩展：新增毛利率、营业利润率、扣非净利率计算；新增瀑布图数据生成；新增现金流三组验证逻辑

---

## [app_V4.3] — 2026-06-13

### 新增

- **累计 NIO vs CFO 图表**：新增累计净利润与累计经营现金流对比图（Part B ④），用于识别"利润赚到纸上"的财务造假风险信号，带填充面积和风险提示文字
- **资产结构扩展分类**：资产堆积图细化为 7 类（固定资产+在建工程、长期股权投资、应收类资产、商誉+无形资产、现金类资产、存货、预付类资产），新增 `_sum_cols()` 辅助函数合并多列

### 改进

- **堆积图改为堆积面积图**：资产结构和负债结构的堆积柱状图（Part D / Part E）改为平滑堆积面积图（`stackgroup:'one'`），匹配参考样式
- **右轴文字重叠修复**：营收图和杜邦分解图移除右轴标题，增加右侧边距（`margin.r:50`），百分比刻度不再与图表内容重叠

---

## [app_V4.2] — 2026-06-13

### 重构

- **前端模板分离**：将 `app_V4.py` 中约 900 行内嵌 HTML/CSS/JS 提取为独立的 `templates/index.html`，Flask 改用 `render_template` + Jinja2 变量，`app_V4.py` 从 ~1828 行精简至 928 行
- **GitHub Pages 落地页**：新增根目录 `index.html` 作为项目介绍页，展示功能特性与本地运行说明

### 改进

- **回归 AKShare 数据源**：所有数据接口统一回归 akshare，移除直接 HTTP API 调用（东方财富 push2、腾讯财经、新浪日线等），代码更简洁可维护
- **PE/PB 历史图表修复**：改用 `ak.stock_zh_a_daily()`（新浪源）替代被代理阻断的 `ak.stock_zh_a_hist()`（东财源），手动重采样为月线，PE/PB 图表正常显示
- **行业信息获取**：从东财 push2 API 改回 `ak.stock_individual_info_em()`
- **股价获取**：从三源链式调用改回 `ak.stock_zh_a_daily()` + `ak.stock_individual_info_em()` 备用

### 移除

- **云端部署相关文件**：删除 `render.yaml`、`index.html`（独立前端）、`scf_bootstrap`、`serverless.yml` 等部署配置，项目回归纯本地运行
- **冗余价格函数**：移除 `_daily_to_monthly`、`_fetch_prices_eastmoney`、`_fetch_prices_tencent`、`_fetch_prices_sina`
- **调试接口**：移除 `/api/debug_sina`、`/api/_debug_price`、`/api/health` 等临时端点

### 修复

- PE/PB 图表空白（`ak.stock_zh_a_hist` 走东财 push2his 被代理阻断）→ 改用 `ak.stock_zh_a_daily`（新浪源）
- `requirements.txt` 补回 `akshare` 依赖

---

## [app_V4.1] — 2026-06-12

### 新增

- **独立前端 `index.html`**：从 Flask 内嵌模板中提取为独立 HTML 文件，可部署到 GitHub Pages
- **Render 云端部署**：新增 `render.yaml` + `requirements.txt`，后端部署至 https://stock-analyzer-x-c.onrender.com
- **CORS 跨域支持**：后端 `app_V4.py` 新增 `Access-Control-Allow-Origin: *` 响应头，允许前端从任意域名调用 API
- **公网可用**：任何人可通过浏览器访问，无需本地运行 Python

### 改进

- **Part B 现金流对照图**：柱状图改为双折线图，去掉比值（ratio）右轴，更直观对比两组数据走势
- **东方财富直接 API**：用 `push2.eastmoney.com` 替代有 bug 的 `stock_individual_info_em`，稳定获取行业名称和总股本
- **PE/PB 口径统一**：历史 PE 和当前 PE 均使用年报 EPS 计算，避免东财"动态 PE"（季度年化）导致百分位失真
- **一致预期年份修正**：优先匹配当前年份（2026）的预测列，而非第一个匹配列

### 修复

- `stock_individual_info_em` pandas 报错（ValueError: Length mismatch）→ 改用东方财富直接 HTTP API
- 一致预期取到 2025 年旧数据 → 优先匹配 2026 列
- PE 百分位 = 0%（动态 PE 与年报 PE 口径不一致）→ 统一用年报 EPS 基准
- `fetch_price()` 备用路径也用 `_get_spot()` 替代已废弃的 `stock_individual_info_em`

---

## [app_V4] — 2026-06-12

### 新增

- **三段式财务分析框架**（对应 `财务分析框架.md`）
  - **Tab ① 业绩检验**
    - KPI 卡片：最新一期营收 / 净利润 / 毛利率 / 净利率 / ROE
    - 营收 & 净利润柱状图 + YoY 增速折线（双轴）
    - 毛利率 / 净利率 / ROE 趋势折线
    - 三组现金流对照图（收入含金量 / 成本含金量 / 利润含金量）
  - **Tab ② 业绩归因**
    - 杜邦三因子分解图（净利率 × 总资产周转 × 权益乘数 = ROE）
    - 杜邦数字明细表（5 年历史）
    - 销售 / 管理 / 研发 / 财务 费用率趋势折线
  - **Tab ③ 验证排雷**
    - 五项排雷仪表盘（红黄绿三色）：利润含金量 / 收入质量 / 库存压力 / 商誉风险 / 偿债风险
    - 应收账款增速 vs 营收增速趋势图
    - 存货增速 vs 营收增速趋势图
  - **Tab ④ 估值区间**（对应 `PE_PB估值法（akshare）.md`）
    - PE 六维矩阵信号卡（TTM PE / 历史分位 / Forward PE / PEG / PB 分位 / EPS 增速）
    - 历史 PE-TTM 折线图（近 5 年 + 中位数线 + 当前值线）
    - 历史 PB 折线图（近 5 年 + 中位数线 + 当前值线）
    - 三角验证目标价区间（低 / 中 / 高）
    - 三档情景分析表（悲观 / 中性 / 乐观）

- 前端改用 **Plotly.js** 交互式图表（CDN 加载），支持悬浮查看数值
- 后端新增 `_get_equity()`：兼容 6 种新浪资产负债表权益列名写法，找不到时用「资产 − 负债」反推
- 后端新增 `_get_pe_history()`：用月线价格 × 年报 EPS/BVPS 本地计算历史 PE/PB 序列
- 后端新增 `compute_valuation()`：PE/PB 矩阵 + 三角验证 + 情景分析完整估值流程

### 修复

- **日期格式 bug**：`_set_date()` 加 `.replace("-", "")` 兼容 `"2016-03-31"` 带横线格式，修复年报过滤全部失败、指标显示 N/A 的问题（由合作者 Claude 检出）
- **权益乘数 / ROE 不显示**：扩展权益列名匹配范围，修复杜邦分解三因子缺失的问题
- **PE/PB 折线图不显示**：显式声明 `xaxis.type:'date'`，去掉 `fill:'tozeroy'` + 透明底色组合（该组合会把线条压到填充层之下）
- **Tab 切换图表贴底**：改为懒渲染，切到对应 Tab 时才调用 Plotly.newPlot()，修复隐藏容器高度为 0 导致所有数据点压在 y=0 的问题
- `fcol()` 改为精确匹配优先，再做子串匹配，减少列名误命中

---

## [app_V1] — 2026-06-11

### 新增

- Flask 单文件 Web 应用（后端 + 内嵌前端）
- 按股票名称（模糊）或 6 位代码精确搜索
- 自动抓取三大报表关键科目（最近 5 期）
- 三种 PE 口径对比：静态（LYR）/ 滚动（TTM）/ 动态，数据源值 vs 手算值对照
- 总市值展示（现价 × 反推总股本）

### 数据源

- AKShare（免费，无需注册）
- 新浪财务接口：三大报表
- 东财快照：行业、现价
- 雪球快照：PE 三种口径（可选，境外网络可能不通）

---

## 框架文档

| 文件 | 作者 | 内容 |
|---|---|---|
| `财务分析框架.md` | 合作者 | 三段式分析方法论：业绩检验 / 业绩归因 / 验证排雷 |
| `业绩检验框架（补充）.md` | 合作者 | 业绩检验细化：A 盈利能力 / B 业绩自洽性 / C 现金流验证（含金量+FCF+三类现金流组合） |
| `验证排雷框架（akshare）.md` | 合作者 | 排雷三维度：A 资产端 / B 偿债杠杆 / C 治理外部 + 红黄绿热力图 + 一票否决 + 行业差异化阈值 |
| `PE_PB估值法（akshare）.md` | 合作者 | PE 矩阵 + 三角验证 + 周期股 PB 调整完整框架 |
| `量价数据框架.md` | 合作者 | 市场印证层：基础行情/资金流向/异动/衍生指标 + 四象限共振判定（横切基本面） |
| `盈利预测框架.md` | 合作者 | 收入两方法(A历史CAGR/B一致预期) + 毛利率三层 + 三档情景 + 敏感性矩阵 + 预期差（含落地说明：方法A+归母净利率锚；方法C量价已删除） |
